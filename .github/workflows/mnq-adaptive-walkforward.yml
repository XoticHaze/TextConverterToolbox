name: mnq-opportunity-target-matrix

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  walkforward:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout research harness
        uses: actions/checkout@v4
      - name: Checkout exact deep MNQ source
        shell: bash
        run: |
          set -euo pipefail
          git clone --filter=blob:none https://github.com/mbytes21/MNQ_DATA.git deep
          git -C deep checkout --detach fc5508e2c152938d6d9eb70a36b888ae26107176
          test "$(git -C deep rev-parse HEAD)" = "fc5508e2c152938d6d9eb70a36b888ae26107176"
          test -f 'deep/plaintext_csv/MNQ 03-20/20191209.Last.csv'
      - name: Install bounded dependencies
        shell: bash
        run: python -m pip install --disable-pip-version-check -q pandas numpy scikit-learn
      - name: Execute predeclared opportunity-target matrix
        shell: bash
        run: |
          set -euo pipefail
          python -m research.mnq_opportunity_target_matrix \
            --deep-root deep/plaintext_csv \
            --output result/mnq-opportunity-target-matrix.json
      - name: Upload metrics only
        uses: actions/upload-artifact@v4
        with:
          name: mnq-opportunity-target-matrix-results
          path: result/mnq-opportunity-target-matrix.json
          if-no-files-found: error
          retention-days: 14
