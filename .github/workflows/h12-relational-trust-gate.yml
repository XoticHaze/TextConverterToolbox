name: h12-relational-trust-gate

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout research harness
        uses: actions/checkout@v4
      - name: Checkout exact pinned AXB source
        shell: bash
        run: |
          set -euo pipefail
          git clone --filter=blob:none https://github.com/axb0306/cme-futures-ohlc.git axb
          git -C axb checkout --detach 60abd3fb6369c6ce0b6a4a65b0f2562fc96b1264
          test "$(git -C axb rev-parse HEAD)" = "60abd3fb6369c6ce0b6a4a65b0f2562fc96b1264"
      - name: Install bounded dependencies
        shell: bash
        run: python -m pip install --disable-pip-version-check -q pandas numpy scikit-learn
      - name: Execute relational trust-gate falsification
        shell: bash
        run: |
          set -euo pipefail
          python -m research.h12_relational_trust_gate --source-root axb --output result/h12-relational-trust-gate.json
      - name: Upload metrics only
        uses: actions/upload-artifact@v4
        with:
          name: h12-relational-trust-gate-results
          path: result/h12-relational-trust-gate.json
          if-no-files-found: error
          retention-days: 14
